{% set name = "legacy-suitcase" %}
{% set version = "0.9.0" %}
{% set sha256 = "7b25f7a936674e55dffc6461e40ca022f2e2c788180cda89a1e6810ee7e00ef8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/bluesky/suitcase-core/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - databroker
    - h5py
    - numpy

test:
  imports:
    - suitcase
    - suitcase.hdf5

about:
  home: https://github.com/bluesky/suitcase-core
  license: BSD-3-Clause
  license_file: LICENSE
  summary: Legacy HDF5 exporter used at NSLS-II ESM-1 and ESM-2
  dev_url: https://github.com/bluesky/suitcase-core
  doc_url: https://blueskyproject.io/suitcase

extra:
  recipe-maintainers:
    - mrakitin
    - kbeeperez
    - conda-forge/event-model
